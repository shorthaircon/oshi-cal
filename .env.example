# 複製這個檔案為 `.env`，填入你自己部署的 Cloudflare Worker URL。
# Worker 的 source code 不在這個 repo 內（已 gitignore），
# 自行參考 https://developers.cloudflare.com/workers/ 部署一個 CORS proxy
# Worker 收到 ?url=<eventernote-url> 後 fetch HTML 加上 CORS header 回傳即可。
VITE_PROXY_URL=https://your-worker.your-account.workers.dev
